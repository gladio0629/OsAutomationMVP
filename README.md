# Axioma Voice OS Control
Python + MCP + Speech Recognition orqali Linux tizimini boshqarish loyihasi.

## O'rnatish
1. `python -m venv venv`
2. `source venv/bin/activate`
3. `pip install -r requirements.txt`
